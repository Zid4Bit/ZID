import { LastnameValidator } from './lastname.validator';

describe('Lastname.Validator', () => {
  it('should create an instance', () => {
    expect(new LastnameValidator()).toBeTruthy();
  });
});
